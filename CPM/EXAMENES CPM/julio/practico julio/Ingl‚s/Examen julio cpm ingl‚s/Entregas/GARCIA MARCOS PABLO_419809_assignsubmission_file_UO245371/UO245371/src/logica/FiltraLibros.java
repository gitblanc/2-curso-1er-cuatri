package logica;

import igu.Library;

public class FiltraLibros {
	public FiltraLibros(){
		
	}
	/*
	public void filtrar(){
		listaDisponibles.clear();
		if(getRadio15().isSelected())
			for(Libro libro : libros)
				if (libro.getPrice() < 15)
					listaDisponibles.addElement(libro.getTitle());
		if(getRadio1540().isSelected())
			for(Libro libro : libros)
				if (libro.getPrice() > 15 && libro.getPrice() < 40)
					listaDisponibles.addElement(libro.getTitle());
		if(Library.getRadio40().isSelected())
			for(Libro libro : libros)
				if (libro.getPrice() > 40)
					listaDisponibles.addElement(libro.getTitle());
		if(getRadioAll().isSelected())
			for(Libro libro : libros)
					listaDisponibles.addElement(libro.getTitle());
	}
	*/
}
