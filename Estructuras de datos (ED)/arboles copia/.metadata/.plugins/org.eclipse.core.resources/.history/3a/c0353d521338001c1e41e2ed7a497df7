/**
 * 
 */
package BST;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

/**
 * @author UO285176
 *
 */
class Tests {

	@Test
	void testSearchNode() {
		BSTree<Integer> tree = new BSTree<>();
		assertNull(tree.searchNode(1));
		assertNull(tree.searchNode(null));
		assertEquals(0, tree.addNode(10));
		assertEquals(tree.addNode(5));
		assertEquals(tree.addNode(2));
		assertEquals(tree.addNode(7));
		assertEquals(tree.addNode(15));
		assertEquals(tree.addNode(12));
	}

}
